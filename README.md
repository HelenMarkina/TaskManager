# Система управления задачами

## 📋 Обзор программы

Консольное приложение "Система управления задачами" позволяет создавать, хранить, просматривать, сортировать, искать задачи, а также сохранять и загружать их из текстовых файлов.


## 📁 Структура данных

### Перечисления (Enums)

#### `Status` - статус задачи
```c
typedef enum {
    NEW,           // 0 - Новая
    IN_PROGRESS,   // 1 - В работе
    COMPLETED,     // 2 - Завершена
    CANCELLED      // 3 - Отменена
} Status;
```

#### `Priority` - приоритет задачи
```c
typedef enum {
    LOW,       // 0 - Низкий
    MEDIUM,    // 1 - Средний
    HIGH,      // 2 - Высокий
    CRITICAL   // 3 - Критический
} Priority;
```

### Структура `Task`
```c
typedef struct {
    char name[100];          // Название задачи
    time_t creation_date;    // Дата создания (время Unix)
    char assignee[50];       // Исполнитель
    Status status;           // Статус задачи
    Priority priority;       // Приоритет задачи
    int estimated_hours;     // Оценка времени выполнения (часы)
    int actual_hours;        // Фактическое время выполнения (часы)
} Task;
```

## 🔧 Реализованные функции

### 1. `int main()`
**Назначение**: Главная функция программы, точка входа  
**Функционал**:
- Инициализация динамического массива задач
- Отображение главного меню
- Обработка выбора пользователя
- Управление памятью

**Возвращает**:
- `0` - успешное завершение
- `1` - ошибка выделения памяти

### 2. `int add_task(Task* tasks, int task_count)`
**Назначение**: Добавление новой задачи в массив  
**Параметры**:
- `tasks` - указатель на массив структур Task
- `task_count` - текущее количество задач

**Функционал**:
- Запрашивает у пользователя данные задачи
- Проверяет корректность ввода
- Добавляет задачу в массив

**Возвращает**:
- `1` - задача успешно добавлена
- `0` - ошибка ввода данных

### 3. `int show_all_tasks(Task* tasks, int task_count)`
**Назначение**: Отображение всех задач  
**Параметры**:
- `tasks` - указатель на массив структур Task
- `task_count` - количество задач для отображения

**Функционал**:
- Выводит подробную информацию о каждой задаче
- Форматирует дату в читаемый вид
- Конвертирует числовые коды статусов и приоритетов в текстовые строки

**Возвращает**: количество отображённых задач

### 4. `int save_to_file(Task* tasks, int task_count)`
**Назначение**: Сохранение задач в текстовый файл  
**Параметры**:
- `tasks` - указатель на массив структур Task
- `task_count` - количество задач для сохранения

**Формат файла**:
```
Название|ДД.ММ.ГГГГ|Исполнитель|СТАТУС|ПРИОРИТЕТ|Оценка|Факт_время
```

**Пример строки**:
```
Разработать интерфейс|15.12.2024|Иванов|IN_PROGRESS|HIGH|40|25
```

**Возвращает**: количество сохранённых задач

### 5. `int load_from_file(Task* tasks, int task_count, int task_capacity)`
**Назначение**: Загрузка задач из текстового файла  
**Параметры**:
- `tasks` - указатель на массив структур Task
- `task_count` - текущее количество задач
- `task_capacity` - максимальная ёмкость массива

**Функционал**:
- Читает строки из файла
- Парсит данные по разделителю '|'
- Конвертирует строковые значения обратно в перечисления
- Проверяет корректность формата данных

**Возвращает**: новое общее количество задач после загрузки

### 6. `int search_by_status(Task* tasks, int task_count)`
**Назначение**: Поиск задач по статусу  
**Параметры**:
- `tasks` - указатель на массив структур Task
- `task_count` - количество задач в массиве

**Функционал**:
- Запрашивает у пользователя статус для поиска
- Выводит задачи с соответствующим статусом
- Отображает краткую информацию о найденных задачах

**Возвращает**: количество найденных задач

### 7. `int search_by_assignee_and_time(Task* tasks, int task_count)`
**Назначение**: Поиск задач по исполнителю и максимальному времени выполнения  
**Параметры**:
- `tasks` - указатель на массив структур Task
- `task_count` - количество задач в массиве

**Функционал**:
- Запрашивает имя исполнителя
- Запрашивает максимальную оценку времени
- Выводит задачи, где исполнитель совпадает и оценка времени не превышает заданную

**Возвращает**: количество найденных задач

### 8. `int sort_tasks(Task* tasks, int task_count)`
**Назначение**: Многоуровневая сортировка задач  
**Параметры**:
- `tasks` - указатель на массив структур Task
- `task_count` - количество задач в массиве

**Алгоритм сортировки**: Пузырьковая сортировка с тремя уровнями сравнения

**Уровни сортировки**:
1. **По оценке времени** (`estimated_hours`) - по возрастанию
2. **По приоритету** (`priority`) - по убыванию (критический → низкий)
3. **По статусу** (`status`) - по убыванию (отменена → новая)

**Возвращает**:
- `1` - массив был отсортирован
- `0` - сортировка не требовалась (задач меньше двух)

### 9. `int show_menu()`
**Назначение**: Отображение главного меню программы  
**Функционал**:
- Выводит список доступных операций
- Получает выбор пользователя
- Очищает буфер ввода

**Возвращает**: выбранный пункт меню


## 📊 Главное меню

```
================================
   СИСТЕМА УПРАВЛЕНИЯ ЗАДАЧАМИ   
================================
1. Показать все задачи
2. Добавить задачу
3. Поиск по статусу
4. Поиск по исполнителю и времени
5. Сортировать задачи
6. Сохранить в файл
7. Загрузить из файла
0. Выход
================================
Выберите пункт меню:
```

## 📝 Пример использования

### Создание задачи
```
=== Добавление новой задачи ===
Введите название задачи: Разработать пользовательский интерфейс
Введите имя исполнителя: Иванов И.И.
Выберите статус задачи (0-3): 1
Выберите приоритет задачи (0-3): 2
Введите оценку времени в часах: 40
Введите фактическое время в часах: 0

Задача успешно создана! Всего задач: 1
```

### Просмотр задач
```
=== Список всех задач ===

--- Задача #1 ---
Название: Разработать пользовательский интерфейс
Дата: 15.12.2024
Исполнитель: Иванов И.И.
Статус: В работе
Приоритет: Высокий
Оценка времени: 40 часов
Фактическое время: 0 часов
```

## 👩‍💻 Информация о разработчике

Маркина Елена, группа бТИИ-251
